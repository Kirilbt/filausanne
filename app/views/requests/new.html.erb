<div class="container">
  <div class="grid">
    <div class="form">
      <p> <%= @sleep.name %> </p>
      <%= simple_form_for [ @sleep, @request ] do |f| %>
        <%= f.submit t('new.request.btn'), class: "btn button-primary" %>
      <% end %>
    </div>
  </div>
</div>
